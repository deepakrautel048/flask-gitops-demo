apiVersion: v2
name: flask-app
description: A Helm chart for the Python Flask GitOps Demo
type: application

# This is the version of the Helm Chart itself
version: 0.1.0

# This is the version of the Python App inside
appVersion: "1.0.0"